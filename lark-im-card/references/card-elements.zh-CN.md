# 卡片元素速查

英文原版: [card-elements.md](card-elements.md)

## `markdown`

用于 Markdown 文本块。

## `div`

用于普通文本块或 `fields` 多栏布局。

## `column_set` 和 `column`

用于比 `div.fields` 更灵活的多列布局。

## `action`

用于按钮组。

## `note`

用于底部备注信息。

## `img`

用于通过 `img_key` 引用已上传图片。
